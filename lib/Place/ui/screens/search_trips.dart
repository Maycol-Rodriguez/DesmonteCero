import 'package:flutter/material.dart';

class mapas_ extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
        child: Image(
      image: AssetImage('assets/01image/app_mapa_calor.png'),
    ));
  }
}
